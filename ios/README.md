# iOS App (planned)

Reserved location for the native iOS app, mirroring `android/`'s role
for the Android app. No code here yet — this is a placeholder directory
only. See the iOS build plan shared in the development conversation for
the intended architecture (a Swift/SwiftUI + WKWebView shell wrapping
the same web bundle as `android/`) before adding anything here.
